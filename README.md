# PEAKA SEMANTICS

Copy the environment file example file:

```
cp .env.example .env
```

Fill the required API keys in .env file

To launch the app locally, just run:

```
streamlit run streamlit.py
```
Your app should start running on port 8501
